# Memory — 2026-06-04

## Session Worklog

### Career Module, Logo Rebrand & Design System (Multiple Sessions)

### Logo v3.3 — Fix SVG external ref not rendering (23:05 ICT)
- **Root cause**: SVG `<image href="logo.jpg">` bị Cloudflare Pages chặn — SVG served với MIME type sai + external raster reference fail silently
- **Fix**: Bỏ SVG wrapper hoàn toàn. Tất cả HTML `<img src="logo.svg|logo-light.svg">` → `logo.jpg` trực tiếp
- SVGs rewritten thành pure vector text (no external refs): logo.svg, logo-light.svg, favicon.svg
- 14 HTML files + admin/admin.html updated
- Commit: `7bf046e`

### Design System v3.0 (22:45 ICT)
- Color palette: Primary #003A99, Secondary #0B6DDB, Accent #FF9900, Text #1A1A1A
- CSS variables, chatbot.css, admin.html, inline HTML colors all updated
- Legacy compat: `--green` → `--secondary`, `--green-dark` → `--primary`
- `assets/css/COLOR-PALETTE.md` created

### Logo Rebrand v3.2 (22:30 ICT)
- SVGs chứa base64 data URI 24KB → không render trên Cloudflare
- Chuyển SVG nhỏ (0.5KB) tham chiếu file logo.jpg riêng
- File mới: `assets/images/logo.jpg` (JPG 17.5KB)
- logo-og.jpg cho OG image / RSS feed
- Commit: `74ef106`

### SEO Gia nhập DCorp (21:10 ICT)
- `_redirects`: `/gia-nhap-dcorp` → career.html, `/gia-nhap-dcorp/:slug` → job-detail.html
- Schema.org JobPosting dynamic JSON-LD (salary, dates, location)
- Sitemap: 6 friendly career URLs
- Dynamic OG/Twitter/Canonical per job
- Career.js links to `/gia-nhap-dcorp/{id}`

### Recruitment Admin Dashboard (20:45 ICT)
- Stats bar, search, filter by status/job
- Detail modal (experience, salary, notes)
- Note modal, inline status change, delete
- Script structure fixed (3 script blocks)

### Audit & Fix (21:20 ICT)
- apply.js: removed extra braces (CRITICAL syntax error)
- apply.js: added experience + salary to newApp (data loss bug)
- apply.js: server-side file validation (type + size)
- career.js: closeDetailModalAndOpenApply function
- Career.html: hidden af-job-title, nav active class, file accept restrict
- job-detail.html: dynamic Schema.org JobPosting
- Noscript fallback, OG image, twitter:image fixes

### Created Pages
- culture.html, benefits.html, gallery.html, job-detail.html
- data/jobs.json (5 positions), data/applications.json, gallery.json
- functions/api/apply.js, functions/api/gallery.js
- `_redirects`, favicon.svg, COLOR-PALETTE.md

### Nav Order
Trang chủ → Giới thiệu → Văn hóa → Phúc lợi → Sản phẩm → Dịch vụ → Thư viện ảnh → Blog → Báo giá → Gia nhập DCorp → Liên hệ

### Known Issues
- GITHUB_TOKEN env var chưa set trong Cloudflare Dashboard → RSS 503, Functions fallback to client-side
- RESEND_API_KEY chưa configure
- TELEGRAM_BOT_TOKEN/CHAT_ID chưa configure
